# API Ports - URL sẽ được tự động detect dựa vào hostname truy cập
# Khi truy cập từ localhost -> dùng localhost
# Khi truy cập từ IP (192.168.x.x) -> dùng IP đó
VITE_API_PORT=3000
VITE_PYTHON_API_PORT=8081

# Fallback URLs (dùng khi không detect được)
VITE_API_BASE_URL=http://localhost:3000/api
VITE_PYTHON_API_URL=http://localhost:8081
