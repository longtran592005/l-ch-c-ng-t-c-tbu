// src/data/mockData.ts
// This file previously contained mock data but has been cleared to ensure data is fetched from the backend API.
import { Schedule, News, Announcement } from '@/types';

export const mockSchedules: Schedule[] = [];
export const mockNews: News[] = [];
export const mockAnnouncements: Announcement[] = [];
export const mockLeaders: any[] = []; // Assuming Leaders type is not defined in '@/types'
export const mockDepartments: any[] = []; // Assuming Departments type is not defined in '@/types'