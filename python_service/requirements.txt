# FastAPI and ASGI server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.9

# Audio processing
faster-whisper==1.0.0
tqdm==4.66.0
pydub==0.25.1
librosa>=0.10.1
soundfile==0.12.1

# FIX PyAV on Windows (avoid build from source)
av==10.0.0 ; platform_system=="Windows"

# Torch (install separately with CUDA)
torch==2.1.2
torchaudio==2.1.2

# Transformers and LLM support
transformers>=4.36.0
accelerate>=0.24.0
scipy>=1.10.0
tokenizers>=0.15.0
psutil>=5.9.0

# Utilities
numpy>=1.24.0
pandas>=2.0.0
python-dotenv>=1.0.0
pyyaml>=6.0

# Logging
colorlog>=6.7.0
# ============================================
# RAG Chatbot Dependencies
# ============================================

# HTTP client for Ollama API
httpx>=0.25.0

# SQL Server connection
pyodbc>=5.0.0

# Document processing
python-docx>=1.0.0

# BitsAndBytes for model quantization (optional, for GPU)
bitsandbytes>=0.41.0 ; platform_system=="Linux"